// PROTOTYPE — Mobile filter window variants
// Delete or absorb when the design direction is decided.
// Question being answered: "What should the mobile filter UI look like?"
// See: frontend/components/FilterPrototype.tsx

import FilterPrototype from '@/components/FilterPrototype';

export default function FilterPrototypePage() {
  return <FilterPrototype />;
}